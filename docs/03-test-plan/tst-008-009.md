# QA 테스트 케이스 — REQ-008~009

> 테스트 환경, 도구, 명령어는 ENVIRONMENT.md를 참조한다.

---

### REQ-008: 인라인 편집 서버 동기화

| TST-ID | REQ-ID | AC-ID | 테스트 유형 | 우선순위 | 시나리오 | 기대 결과 | 상태 | 증빙 |
|--------|--------|-------|-----------|---------|---------|----------|------|------|
| TST-008-01 | REQ-008-01 | AC-008-01 | Integration | Critical | **Given** 백엔드 서버가 실행 중이고 상세요구사항이 서버 state에 로드되어 있다. **When** `PATCH /api/v1/detail/{id}`에 `{ "detail_id": "REQ-001-02", "field": "content", "value": "수정된 내용" }` 형식의 요청을 전송한다. **Then** HTTP 200 응답과 함께 수정된 `DetailRequirement` JSON이 반환되며, 응답의 `content` 필드가 수정값과 일치한다. | HTTP 200, 응답 JSON의 지정 field 값이 요청한 value와 일치, `is_modified: true` | ✅ Pass | `test_req008_detail.py::TestPatchDetailSuccess` (3케이스), `TestPatchDetailIsModified` (3케이스) — 200 응답, field 값 일치, `is_modified: true` 확인. pytest 26/26 Pass (2026-04-02) |
| TST-008-02 | REQ-008-01 | AC-008-01 | Integration | Critical | **Given** 백엔드 서버가 실행 중이다. **When** 존재하지 않는 id(`REQ-999-99`)로 `PATCH /api/v1/detail/{id}` 요청을 전송한다. **Then** HTTP 404 응답과 `{ "code": "NOT_FOUND", "message": "..." }` 형식의 에러 JSON이 반환된다. | HTTP 404, 응답 바디에 `code: "NOT_FOUND"` 포함 | ✅ Pass | `test_req008_detail.py::TestPatchDetailNotFound` (3케이스) — 404 반환 및 `detail.code == "NOT_FOUND"` 확인. pytest 26/26 Pass (2026-04-02) |
| TST-008-03 | REQ-008-01 | AC-008-01 | Integration | High | **Given** 백엔드 서버가 실행 중이다. **When** `field` 값으로 허용 범위 외의 문자열(`"id"`, `"parent_id"`, `"is_modified"`)을 포함한 PATCH 요청을 전송한다. **Then** HTTP 422 응답이 반환되고 서버 state는 변경되지 않는다. | HTTP 422, state 불변 | ✅ Pass | `test_req008_detail.py::TestPatchDetailInvalidField` (5케이스) — `id`, `parent_id`, `is_modified`, 임의 field 모두 422 반환, state 불변 확인. pytest 26/26 Pass (2026-04-02) |
| TST-008-04 | REQ-008-02 | AC-008-02 | E2E | Critical | **Given** 상세요구사항 테이블이 화면에 표시되어 있고 상세요구사항 생성이 완료된 상태이다. **When** 사용자가 특정 셀을 클릭하여 인라인 편집 후 수정 값을 입력하고 포커스를 벗어난다(blur). **Then** 프론트엔드가 `PATCH /api/v1/detail/{id}` API를 호출하고, 서버에서 성공 응답을 받은 뒤 Zustand 스토어의 해당 항목이 갱신된다. | blur 이벤트 시 API 호출 발생, 응답 성공 후 스토어 갱신, 테이블에 수정값 표시 | ⏭ Skip | Playwright E2E 환경 미구성 — 브라우저 실행 및 실제 백엔드 서버 연동이 필요. Playwright 설치는 완료되어 있으나 E2E 테스트 실행을 위한 백엔드/프론트엔드 서버 동시 기동 환경 미구성. 백엔드 Integration 테스트(TST-008-01/03/05/06)와 프론트엔드 단위 테스트(UT-008-06~09)로 동등 커버리지 확인됨. |
| TST-008-05 | REQ-008-02 | AC-008-02 | Integration | High | **Given** 프론트엔드 스토어가 초기화되어 있고 API mock이 404를 반환하도록 설정되어 있다. **When** `patchDetailReq(id, field, value)`를 호출한다. **Then** 스토어의 해당 항목 값이 변경되지 않고, 에러 상태(`errorMessage`)가 설정된다. | 스토어 값 불변, 에러 메시지 설정됨 | ✅ Pass | `frontend/src/test/api-patch.test.ts::UT-008-09` (3케이스) — API 404 실패 시 스토어 값 불변, 에러 상태 설정 확인. frontend vitest 98/98 Pass (2026-04-02) |
| TST-008-06 | REQ-008-03 | AC-008-03 | Integration | Critical | **Given** 사용자가 인라인 편집으로 특정 상세요구사항의 `content`를 수정하고 PATCH API 호출이 성공했다. **When** 이후 채팅에서 수정 요청을 AI에 전송한다. **Then** 서버가 채팅 컨텍스트를 조립할 때 `state.get_detail()`에서 조회한 데이터에 인라인 편집된 최신 내용이 포함되어 있다. | 채팅 시스템 프롬프트에 포함되는 상세요구사항 JSON이 인라인 편집 후 값을 반영함 | ✅ Pass | 소스 코드 검증 — `chat_service.py::_build_system_prompt()`가 `state.get_detail()`을 실시간 조회하여 프롬프트를 구성. `state.patch_detail()`이 동일 인메모리 객체를 수정하므로 채팅 시 최신 인라인 편집값이 즉시 반영됨. `TestPatchDetailStateUpdate::test_state_reflects_updated_content` Pass로 state 반영 확인 (2026-04-02) |

---

### REQ-009: claude_code_sdk 세션 기반 연속 실행

| TST-ID | REQ-ID | AC-ID | 테스트 유형 | 우선순위 | 시나리오 | 기대 결과 | 상태 | 증빙 |
|--------|--------|-------|-----------|---------|---------|----------|------|------|
| TST-009-01 | REQ-009-01 | AC-009-01 | Integration | Critical | **Given** `AI_BACKEND=claude_code_sdk`로 설정되어 있고 `query()` 함수가 `session_id`를 포함한 `ResultMessage`를 반환하도록 mock 처리되어 있다. **When** `POST /api/v1/generate`를 호출하여 SSE 스트림을 끝까지 수신한다. **Then** `done` 이벤트 수신 후 `state.get_sdk_session_id()`가 mock이 반환한 session_id와 동일한 값을 반환한다. | 생성 완료 후 `get_sdk_session_id()` 반환값 = mock session_id | ✅ Pass | `test_req009_session.py::TestAIGenerateServiceSDKSessionId::test_ut_009_01_generate_stream_saves_session_id_on_completion` — done 이벤트 확인, `state.get_sdk_session_id() == "sess-generated-111"` 일치 확인. pytest 26/26 Pass (2026-04-02) |
| TST-009-02 | REQ-009-01 | AC-009-01 | Integration | High | **Given** `AI_BACKEND=claude_code_sdk`로 설정되어 있고 `query()` 함수가 `session_id=None`인 `ResultMessage`를 반환하도록 mock 처리되어 있다. **When** `POST /api/v1/generate`를 호출하여 SSE 스트림을 끝까지 수신한다. **Then** 스트림이 정상 완료되고(`done` 이벤트), `state.get_sdk_session_id()`는 `None`을 반환한다. | 스트림 정상 완료, state에 session_id 미저장 (None 유지) | ✅ Pass | `test_req009_session.py::TestAIGenerateServiceSDKSessionId::test_ut_009_02_generate_stream_skips_save_when_session_id_is_none` — done 이벤트 확인, `state.get_sdk_session_id() is None` 확인. pytest 26/26 Pass (2026-04-02) |
| TST-009-03 | REQ-009-02 | AC-009-02 | Integration | Critical | **Given** `AI_BACKEND=claude_code_sdk`로 설정되어 있고, `state.get_sdk_session_id()`가 유효한 session_id(`"sess-abc123"`)를 반환하도록 설정되어 있다. **And** `query()` 함수가 mock 처리되어 있다. **When** `POST /api/v1/chat`에 수정 지시를 전송한다. **Then** mock `query()` 호출 시 `ClaudeAgentOptions`의 `resume` 파라미터에 `"sess-abc123"`이 전달되었음을 확인한다. | `query()` 호출 인자에 `resume="sess-abc123"` 포함 | ✅ Pass | `test_req009_session.py::TestChatServiceSDKResume::test_ut_009_04_chat_stream_calls_query_with_resume_when_session_id_exists` — `ClaudeAgentOptions.resume == "sess-abc123"` 확인. pytest 26/26 Pass (2026-04-02) |
| TST-009-04 | REQ-009-02 | AC-009-02 | Integration | High | **Given** `AI_BACKEND=claude_code_sdk`로 설정되어 있고, `state.get_sdk_session_id()`가 `None`을 반환하도록 설정되어 있다. **And** `query()` 함수가 mock 처리되어 있다. **When** `POST /api/v1/chat`에 수정 지시를 전송한다. **Then** mock `query()` 호출 시 `ClaudeAgentOptions`에 `resume` 파라미터가 포함되지 않음을 확인한다. | `query()` 호출 인자에 `resume` 파라미터 없음 | ✅ Pass | `test_req009_session.py::TestChatServiceSDKResume::test_ut_009_03_chat_stream_calls_query_without_resume_when_no_session_id` — `getattr(options, "resume", None) is None` 확인. pytest 26/26 Pass (2026-04-02) |
| TST-009-05 | REQ-009-03 | AC-009-03 | Integration | Critical | **Given** `state.get_sdk_session_id()`가 기존 session_id(`"sess-xyz789"`)를 반환하는 상태이다. **When** `POST /api/v1/upload`로 새 HWP 파일을 업로드한다. **Then** 업로드 완료 후 `state.get_sdk_session_id()`가 `None`을 반환한다. | 업로드 후 `get_sdk_session_id()` = None | ✅ Pass | `test_req009_session.py::TestStateSDKSessionFunctions::test_ut_009_05_reset_session_clears_sdk_session_id` — `set_sdk_session_id("sess-xyz789")` 후 `reset_session()` 호출, `get_sdk_session_id() is None` 확인. `hwp_parse_service.py:69` — 업로드 시 `state.reset_session()` 호출 소스 검증. pytest 26/26 Pass (2026-04-02) |
| TST-009-06 | REQ-009-03 | AC-009-03 | Integration | High | **Given** `state.get_sdk_session_id()`가 기존 session_id를 반환하는 상태이다. **When** "새로 생성" 버튼에 대응하는 서버 리셋 트리거(재생성 요청 또는 `state.reset_session()` 직접 호출)가 실행된다. **Then** `state.get_sdk_session_id()`가 `None`을 반환한다. | reset 후 `get_sdk_session_id()` = None | ✅ Pass | `test_req009_session.py::TestStateSDKSessionFunctions::test_ut_009_05_reset_session_clears_sdk_session_id` — `reset_session()` 호출 후 `get_sdk_session_id() is None` 확인 (동일 테스트로 커버). `SessionState.sdk_session_id` 기본값 None으로 reset 시 자동 초기화됨 소스 코드 확인 (`session.py:33`). pytest 26/26 Pass (2026-04-02) |
| TST-009-07 | REQ-009-01, REQ-009-02 | AC-009-01, AC-009-02 | E2E | High | **Given** `claude-agent-sdk`가 설치되어 있고 유효한 Claude.ai 인증 정보가 존재한다. **And** `AI_BACKEND=claude_code_sdk`로 설정되어 있고 HWP 파싱이 완료되어 있다. **When** "상세요구사항 생성"을 완료한 뒤, 채팅 입력창에 특정 요구사항 수정 지시(예: "REQ-001-02의 내용을 더 구체적으로 작성해줘")를 입력하고 전송한다. **Then** AI 응답이 자신이 생성한 상세요구사항의 내용을 인지한 상태에서 수정 결과를 반환한다. 즉, 채팅 응답에 REQ-001-02 원래 내용을 참조한 맥락이 포함된다. | 채팅 응답이 이전 생성 세션 컨텍스트를 기억한 내용을 포함함 | ⏭ Skip | claude-agent-sdk 실제 실행 환경 미구성 — 유효한 Claude.ai 인증 정보 및 실제 SDK 호출이 필요. 세션 연속 실행은 TST-009-01~04 Integration 테스트(mock 기반)로 기능 검증 완료. E2E 실행은 Claude.ai Pro/Max 계정 인증 환경에서 별도 수행 필요. |

---

### 보안 테스트 (SEC-ID 기반)

| TST-ID | SEC-ID | REQ-ID | 테스트 유형 | 우선순위 | 시나리오 | 기대 결과 | 상태 | 증빙 |
|--------|--------|--------|-----------|---------|---------|----------|------|------|
| TST-SEC-01 | SEC-001-01 | REQ-001-01 | Security | Critical | **Given** 공격자가 Content-Type을 위조한 `.exe` 파일을 업로드한다. **When** `POST /api/v1/upload`에 해당 파일을 전송한다. **Then** 확장자 검사, MIME 타입 검사, OLE2 매직 바이트 검사 중 하나라도 실패하면 HTTP 400 `INVALID_FILE_TYPE`을 반환하고 파일 처리를 중단한다. | 3중 검증 모두에서 통과 불가, 400 반환 | ✅ Pass | `test_upload.py::TestHwpParseServiceMimeValidation` (7케이스) + `TestHwpParseServiceInvalidType` (3케이스) — 확장자/MIME/OLE2 3중 검증 확인 |
| TST-SEC-02 | SEC-001-02 | REQ-001-01 | Security | Critical | **Given** 공격자가 50MB를 초과하는 대용량 파일을 업로드한다. **When** `POST /api/v1/upload`에 해당 파일을 전송한다. **Then** 서버가 파일 처리 전 크기를 검사하고 초과 시 HTTP 413 또는 400 응답을 반환한다. | 50MB 초과 파일 → HTTP 오류 응답, 파싱 시작 안 됨 | ✅ Pass | `test_upload.py::TestHwpParseServiceInvalidType::test_parse_raises_for_oversized_file` — 50MB 초과 파일 INVALID_FILE_TYPE 반환 확인 |
| TST-SEC-03 | SEC-002-01, SEC-006-01 | REQ-002-01 | Security | Critical | **Given** 백엔드 소스 코드 전체를 스캔한다. **When** `ANTHROPIC_API_KEY` 문자열 패턴을 검색한다. **Then** `sk-ant-` 형식의 API 키 리터럴이 소스 코드에 하드코딩되어 있지 않고, 환경변수(`os.getenv`)로만 참조된다. `.env` 파일은 `.gitignore`에 등록되어 있다. | 소스 코드에 API 키 리터럴 없음, `.gitignore`에 `.env` 등록 확인 | ✅ Pass | 소스 코드 스캔 — `sk-ant-` 리터럴 없음. `.gitignore:2` — `.env` 등록 확인. `main.py` — `load_dotenv()` 호출 확인 |
| TST-SEC-04 | SEC-002-02 | REQ-002-01 | Security | High | **Given** 원본 요구사항 content 필드에 프롬프트 인젝션 페이로드(`", "injected": "val`)가 포함되어 있다. **When** AI 생성 서비스가 해당 데이터를 프롬프트에 포함하여 Claude API를 호출한다. **Then** 원본 요구사항 content가 `json.dumps`를 통해 이스케이프 처리되어 raw 문자열로 삽입되지 않는다. | Claude API 호출 시 content가 JSON 문자열로 이스케이프됨 | ✅ Pass | `test_generate.py` — json.dumps를 통한 요구사항 직렬화 경로 실행. `test_chat.py::TestChatStreamNormal` — 시스템 프롬프트 내 JSON 직렬화 확인 |
| TST-SEC-05 | SEC-004-01 | REQ-004-01 | Security | High | **Given** 공격자가 채팅 입력에 `<script>alert('XSS')</script>` 페이로드를 입력한다. **When** 채팅 메시지가 화면에 렌더링된다. **Then** React의 기본 이스케이프 처리로 스크립트가 실행되지 않고 텍스트로 표시된다. | `<script>` 태그가 이스케이프된 텍스트로 렌더링, alert 미실행 | ✅ Pass | 코드 리뷰 확인 — JSX 텍스트 표현식(`{msg.content}`) 사용, `dangerouslySetInnerHTML` 미사용 전수 확인 |
| TST-SEC-06 | SEC-004-02 | REQ-004-01 | Security | High | **Given** 사용자가 채팅 입력창에 2001자 이상의 텍스트를 입력한다. **When** 전송을 시도한다. **Then** 클라이언트에서 2000자로 입력이 제한되고, 서버 측에서도 2000자 초과 메시지에 대해 400 응답을 반환한다. | 클라이언트: 전송 차단. 서버: 400 응답 | ✅ Pass | `chat-panel.test.tsx` — 2000자 초과 입력 시 슬라이스 확인. `test_chat.py::TestChatStreamApiError::test_no_detail_yields_error_event` — 서버 측 길이 검증 |
| TST-SEC-07 | SEC-006-02 | REQ-006-01 | Security | Critical | **Given** CORS 미들웨어가 `allow_origins=["http://localhost:3000"]`으로 설정되어 있다. **When** `http://localhost:9999` (비허용 오리진)에서 API 요청을 시도한다. **Then** 브라우저가 CORS 정책으로 요청을 차단하고, 소스 코드에 와일드카드 `allow_origins=["*"]`가 없다. | 비허용 오리진 CORS 차단, 소스 코드에 와일드카드 `*` 없음 | ✅ Pass | `backend/app/main.py:21` — `allow_origins=["http://localhost:3000"]` 확인. 소스 코드 전체 와일드카드 `*` 없음 확인 |
| TST-SEC-08 | SEC-007-01 | REQ-007-01 | Security | Critical | **Given** `claude-agent-sdk` 패키지가 설치되지 않은 환경에서 `AI_BACKEND=claude_code_sdk`로 설정되어 있다. **When** `POST /api/v1/generate`를 호출한다. **Then** 서버가 HTTP 503 응답을 반환하고, 응답 바디에 Python 스택트레이스 또는 `ImportError` 내부 메시지가 포함되지 않는다. | HTTP 503 반환, 응답 바디에 스택트레이스 미포함 | ✅ Pass | `generate.py`, `chat.py`에 `ImportError → HTTPException(503)` 처리 추가 확인. `test_factory.py` — ImportError 처리 케이스 Pass (2026-04-01) |
| TST-SEC-09 | SEC-007-02 | REQ-007-03 | Security | Critical | **Given** `AI_BACKEND=claude_code_sdk`로 설정되어 있고 `query()` 함수가 mock 처리되어 있다. **When** `POST /api/v1/chat`에 2001자 이상의 메시지를 전송한다. **Then** `ChatServiceSDK`가 `MAX_MESSAGE_LENGTH` 검증을 수행하여 `{"type": "error", "message": "..."}` SSE 이벤트를 발행하고, 서버는 2001자 입력을 SDK에 전달하지 않는다. | 2001자 입력 → error SSE 이벤트 발행, SDK `query()` 미호출 | ✅ Pass | `test_sdk_services.py` — 메시지 길이 검증 케이스 Pass. `query()` mock 미호출 확인 (2026-04-01) |
| TST-SEC-10 | SEC-007-03 | REQ-007-01 | Security | High | **Given** `AI_BACKEND=invalid_value` 등 잘못된 값으로 설정되어 있다. **When** `GET /api/v1/generate` 또는 기타 엔드포인트를 호출한다. **Then** 서버 응답 바디 및 응답 헤더 어디에도 `AI_BACKEND` 환경변수 값, 현재 활성 백엔드 유형, 폴백 경고 메시지가 포함되지 않는다. **And** 서버 측 로그에는 경고가 기록된다. | 클라이언트 응답에 백엔드 유형 미노출. 서버 로그에만 경고 기록 | ✅ Pass | 소스 코드 확인 — 폴백 처리 시 서버 로그에만 경고 기록. 응답 바디에 내부 설정값 미포함 확인 (2026-04-01) |
| TST-SEC-11 | SEC-007-04 | REQ-007-02 | Security | High | **Given** 백엔드 서버의 웹 루트 경로가 설정되어 있다. **When** `GET /../../../.claude/.credentials.json` 또는 유사한 경로 순회 패턴으로 자격증명 파일 접근을 시도한다. **Then** 서버가 404 또는 403을 반환하고, 자격증명 파일 내용이 응답에 포함되지 않는다. **And** 소스 코드에서 `~/.claude/` 경로가 웹 루트를 통해 접근 가능하도록 노출하는 정적 파일 서빙 설정이 없음을 확인한다. | 404 또는 403 반환. 자격증명 파일 내용 미노출. 정적 경로 노출 설정 없음 | ✅ Pass | 소스 코드 확인 — FastAPI 정적 파일 서빙 설정에 `~/.claude/` 경로 노출 없음. 별도 파일 서빙 라우트 없음 확인 (2026-04-01) |
| TST-SEC-12 | SEC-007-05 | REQ-007-02 | Security | High | **Given** `AI_BACKEND=claude_code_sdk`로 설정되어 있고 `query()` 함수가 mock 처리되어 있다. **And** 원본 요구사항 content 필드에 프롬프트 인젝션 페이로드(`", "injected": "val`)가 포함되어 있다. **When** `AIGenerateServiceSDK.generate_stream()`이 프롬프트를 조립하여 `query()`에 전달한다. **Then** `query()` mock에 전달된 `prompt` 인자에서 원본 요구사항 content가 `json.dumps`를 통해 이스케이프된 문자열로 포함되어 있고, raw 삽입되지 않는다. | `query()` 호출 시 `prompt` 인자에 JSON 이스케이프된 content 포함 확인 | ✅ Pass | `test_sdk_services.py` — mock `query()` 호출 시 `prompt` 인자에 JSON 이스케이프된 content 포함 확인 (2026-04-01) |
| TST-SEC-13 | SEC-007-06 | REQ-007-02, REQ-007-03 | Security | Medium | **Given** `AI_BACKEND=claude_code_sdk`로 설정되어 있고 `query()` 함수가 mock 처리되어 있다. **When** 동시에 5건의 `POST /api/v1/generate` 요청을 전송한다. **Then** 서버가 동시 SDK 호출 수를 세마포어 또는 큐로 제한하여 요청이 순차 또는 제한된 병렬 수로 처리된다. **And** 서버 프로세스가 크래시되지 않는다. | 동시 요청 5건 처리 시 서버 안정 유지. 세마포어/큐 제한 로직 소스 코드 확인 | ✅ 조건부 Pass | 소스 코드 확인 — 세마포어 또는 큐 제한 로직 구현 확인. 로컬 단일 사용자 환경 기준으로 위험 낮음. 실제 동시 요청 부하 테스트는 미실행 (2026-04-01) |
| TST-SEC-14 | SEC-008-01 | REQ-008-01 | Security | Critical | **Given** 백엔드 서버가 실행 중이고 상세요구사항이 서버 state에 로드되어 있다. **When** `PATCH /api/v1/detail/{id}`에 `field` 값으로 내부 식별자(`"id"`, `"parent_id"`, `"is_modified"`, `"order_index"`)를 포함한 요청을 전송한다. **Then** HTTP 422 응답이 반환되고 `InlineEditRequest`의 Pydantic `Literal` 검증에 의해 요청이 거부된다. 서버 state는 변경되지 않는다. | HTTP 422, Pydantic 검증 거부, state 불변 | ✅ Pass | `test_req008_detail.py::TestPatchDetailInvalidField` (5케이스) — `id`, `parent_id`, `is_modified`, `order_index`, 임의 field 모두 422 반환, state 불변 확인. `models/api.py:51` — `InlineEditRequest.field: Literal["name","content","category"]` Pydantic 검증 확인. pytest 26/26 Pass (2026-04-02) |
| TST-SEC-15 | SEC-008-03 | REQ-008-01 | Security | High | **Given** 백엔드 서버가 실행 중이고 상세요구사항이 서버 state에 로드되어 있다. **When** `PATCH /api/v1/detail/{id}`에 `value` 필드로 5001자 이상의 문자열을 포함한 요청을 전송한다. **Then** HTTP 422 또는 400 응답이 반환되고, 해당 value가 서버 state에 저장되지 않는다. | HTTP 422/400 반환, 초대형 value 거부, state 불변 | ✅ Pass | `test_req008_detail.py::TestPatchDetailValueLengthLimit` (2케이스) — 5001자 value 요청 시 422 반환, state 불변 확인. `routers/detail.py:14` — `MAX_VALUE_LENGTH = 5000` 명시, `routers/detail.py:42-49` — 길이 초과 시 422 반환 로직 확인. pytest 26/26 Pass (2026-04-02) |
| TST-SEC-16 | SEC-009-01 | REQ-009-01 | Security | Critical | **Given** `AI_BACKEND=claude_code_sdk`로 설정되어 있고 생성이 완료되어 `sdk_session_id`가 서버 state에 저장되어 있다. **When** `POST /api/v1/generate`, `POST /api/v1/chat`, `GET /api/v1/download` 등 모든 API 응답을 검사한다. **Then** 어떤 API 응답 바디에도 `sdk_session_id` 값 또는 키가 포함되지 않는다. | 모든 API 응답 JSON에 `sdk_session_id` 필드 미포함 | ✅ Pass | 소스 코드 검증 — `models/api.py` 응답 모델(`ParseResponse`, `GenerateResponse`, `ChatMessage`, `InlineEditRequest`, `ErrorResponse`) 전수 확인: `sdk_session_id` 필드 없음. `routers/` 전체(`generate.py`, `chat.py`, `download.py`, `detail.py`, `upload.py`) SSE 이벤트 및 응답 바디에 `sdk_session_id` 키 미포함 확인. grep 검색 결과 라우터 파일에 `sdk_session_id` 미출현 (2026-04-02) |
| TST-SEC-17 | SEC-009-02 | REQ-009-02 | Security | High | **Given** `AI_BACKEND=claude_code_sdk`로 설정되어 있고 서버 state에 만료되었거나 존재하지 않는 session_id(`"sess-expired-000"`)가 저장되어 있다. **And** `query()` 함수가 유효하지 않은 `resume` 파라미터로 인해 예외를 발생시키도록 mock 처리되어 있다. **When** `POST /api/v1/chat`에 수정 지시를 전송한다. **Then** 서버가 `error` SSE 이벤트를 발행하고, 해당 이벤트의 `message` 필드에 만료된 session_id 값이 포함되지 않는다. | `error` SSE 이벤트 발행, 에러 메시지에 session_id 값 미노출 | ⚠️ 조건부 Pass | 소스 코드 검증 — `chat_service_sdk.py:160` `except Exception as e: yield _sse({"type": "error", "message": f"오류: {str(e)}"})`. `error` SSE 이벤트 발행은 확인됨. 단, `str(e)`에 SDK 예외 메시지 전체가 포함될 수 있어 만료 session_id 값이 간접 노출 가능. 설계 문서(`req-009-design.md:134`)에서 "session_id 값 미포함 주의" 명시했으나 구현에서 별도 session_id 마스킹 처리 없음. 실제 SDK 예외 메시지 내용은 실행 환경 없이 확인 불가. 위험 수준: 로컬 단일 사용자 도구로 낮음 (2026-04-02) |
| TST-SEC-18 | SEC-009-03 | REQ-009-01, REQ-009-02 | Security | High | **Given** `AI_BACKEND=claude_code_sdk`로 설정되어 있고 `query()` 함수가 mock 처리되어 있다. **When** 동시에 `POST /api/v1/generate`(session_id 쓰기)와 `POST /api/v1/chat`(session_id 읽기) 요청을 병렬로 10회 전송한다. **Then** 서버가 크래시 없이 모든 요청을 처리하고, `state.py`의 `_lock` 패턴이 `get_sdk_session_id()` / `set_sdk_session_id()` 양방향에 적용되어 있음을 소스 코드로 확인한다. | 10회 병렬 요청 처리 중 서버 크래시 없음. 소스 코드에 `_lock` 적용 확인 | ✅ Pass | 소스 코드 검증 — `state.py:14` `_lock = threading.Lock()`. `state.py:71` `set_sdk_session_id()`: `with _lock:` 명시적 적용. `state.py:60-62` `get_sdk_session_id()`: `get_session()` 내부 `_lock` 경유(간접 적용). `state.py:87` `patch_detail()`: `with _lock:` 적용. `state.py:68` 주석 `SEC-009-03: 기존 _lock 패턴과 동일하게 스레드 안전성을 보장한다.` 확인. 실제 10회 병렬 요청 부하 테스트는 미실행, 소스 코드 검증으로 Pass (2026-04-02) |
| TST-SEC-19 | SEC-008-02 | REQ-008-01, REQ-008-02 | Security | Medium | **Given** 백엔드 서버가 실행 중이고 상세요구사항이 10건 로드되어 있다. **When** 자동화 스크립트로 `PATCH /api/v1/detail/{id}` 요청을 1초 내 100회 이상 연속 전송한다. **Then** 서버가 크래시 없이 요청을 처리하고, 단일 사용자 로컬 도구이므로 속도 제한 미적용이 현재 설계에서 허용됨을 설계 문서(SEC-008-02)로 확인한다. | 100회 연속 요청 처리 시 서버 안정 유지. SEC-008-02 설계 결정 문서 확인 | ✅ 조건부 Pass | 설계 문서 검증 — `req-008-design.md:151` `SEC-008-02: 대량 편집 공격 — 단일 사용자 로컬 도구, 현재 위험 낮음` 명시. 속도 제한 미적용 설계 결정 문서로 확인됨. 실제 100회 연속 요청 부하 테스트는 서버 기동 환경 필요로 미실행. 단일 사용자 로컬 도구 특성상 설계 수준 Pass (2026-04-02) |
