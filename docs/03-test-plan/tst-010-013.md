# QA 테스트 케이스 — REQ-010~013

> 테스트 환경, 도구, 명령어는 ENVIRONMENT.md를 참조한다.

---

### REQ-010: 생성 진행률 표시

| TST-ID | REQ-ID | AC-ID | 테스트 유형 | 우선순위 | 시나리오 | 기대 결과 | 상태 | 증빙 |
|--------|--------|-------|-----------|---------|---------|----------|------|------|
| TST-010-01 | REQ-010-01 | AC-010-01 | Integration | High | **Given** `AI_BACKEND=claude_code_sdk`로 설정되어 있고 `query()` 함수가 mock 처리되어 있으며, 원본 요구사항이 3건 서버 state에 로드되어 있다. **When** `POST /api/v1/generate`를 호출하여 SSE 스트림을 끝까지 수신한다. **Then** 스트림 내에 `type: "progress"` 이벤트가 3건 수신되며, 각 이벤트의 `current`가 1~3 순서로 증가하고, `total`이 3이며, `req_id`가 원본 요구사항 ID와 일치한다. | SSE 스트림에 `progress` 이벤트 3건 포함. `current` 1-based 순증, `total=3`, `req_id` 각 원본 ID와 일치 | 미실행 | |
| TST-010-02 | REQ-010-01 | AC-010-01 | Integration | High | **Given** `AI_BACKEND=anthropic_api`로 설정되어 있고 Anthropic API가 mock 처리되어 있으며, 원본 요구사항이 2건 로드되어 있다. **When** `POST /api/v1/generate`를 호출하여 SSE 스트림을 끝까지 수신한다. **Then** 스트림 내에 `type: "progress"` 이벤트가 2건 수신되며, 이벤트 구조(`current`, `total`, `req_id`)가 SDK 경로와 동일하다. | 두 AI 백엔드 모두 동일한 `progress` SSE 이벤트 형식으로 응답 | 미실행 | |
| TST-010-03 | REQ-010-02 | AC-010-02 | E2E | High | **Given** HWP 파싱이 완료되어 있고 원본 요구사항이 5건 화면에 표시되어 있다. **When** 사용자가 "상세요구사항 생성" 버튼을 클릭하고 `progress` SSE 이벤트가 수신된다. **Then** 화면에 "N / M 항목 생성 중 (REQ-NNN)" 형태의 텍스트와 진행률 바가 표시되며, 새 `progress` 이벤트 수신 시마다 N 값과 진행률 바가 갱신된다. | `isGenerating=true` + `progressTotal>0` 상태에서 진행률 텍스트 및 진행률 바 표시. progress 이벤트 수신 시 UI 갱신 | 미실행 | |
| TST-010-04 | REQ-010-03 | AC-010-03 | E2E | High | **Given** 상세요구사항 생성이 진행 중이고 진행률 UI가 화면에 표시되어 있다. **When** SSE 스트림에서 `done` 이벤트가 수신되어 생성이 완료된다. **Then** 진행률 텍스트와 진행률 바가 DOM에서 사라지고, 생성된 상세요구사항 테이블이 정상 표시된다. | `isGenerating=false` 전환 후 진행률 UI 비표시. 최종 결과 테이블 표시 | 미실행 | |

---

### REQ-011: 채팅창 Sticky 고정

| TST-ID | REQ-ID | AC-ID | 테스트 유형 | 우선순위 | 시나리오 | 기대 결과 | 상태 | 증빙 |
|--------|--------|-------|-----------|---------|---------|----------|------|------|
| TST-011-01 | REQ-011-01 | AC-011-01 | E2E | Critical | **Given** 원본 요구사항 테이블과 상세요구사항 테이블이 화면에 표시되어 있고 콘텐츠가 뷰포트 높이를 초과한다. **When** 사용자가 페이지를 아래로 스크롤한다. **Then** `#chat-area` 요소의 `position` 스타일이 `sticky`이고, 채팅창이 뷰포트 우측에 고정되어 화면에서 사라지지 않는다. DOM에서 `position: sticky` 및 `top` 속성 값을 확인한다. | 스크롤 후에도 `#chat-area`가 뷰포트 내에 위치. `getComputedStyle(#chat-area).position === 'sticky'` | 미실행 | |
| TST-011-02 | REQ-011-02 | AC-011-02 | E2E | Critical | **Given** 채팅창이 sticky 상태로 화면에 고정되어 있고, 페이지가 아래로 스크롤된 상태이다. **When** 사용자가 채팅 입력창에 텍스트를 입력하고 전송 버튼을 클릭한다. **Then** `POST /api/v1/chat` 요청이 발생하고 AI 응답이 채팅창에 정상 표시된다. 채팅 메시지 전송 기능이 sticky 위치에서 정상 동작한다. | 채팅 전송 버튼 클릭 시 `handleSend` 실행, `/api/v1/chat` 요청 발생, AI 응답 채팅창에 표시 | 미실행 | |
| TST-011-03 | REQ-011-03 | AC-011-03 | E2E | High | **Given** 채팅창이 sticky 상태로 화면 우측에 고정되어 있다. **When** 사용자가 좌측 테이블 영역을 스크롤하고 테이블 행을 클릭하여 인라인 편집을 시도한다. **Then** `#chat-area`가 테이블 콘텐츠를 가리지 않으며, 테이블의 모든 행이 스크롤 접근 가능하고 편집 가능하다. `#table-area`의 너비가 `#chat-area` width만큼 줄어들지 않고 레이아웃이 유지된다. | 테이블 행 전체 클릭 및 편집 가능. `#table-area`와 `#chat-area` 겹침 없음. 스크린샷으로 레이아웃 확인 | 미실행 | |

---

### REQ-012: 상세요구사항 행 삭제

| TST-ID | REQ-ID | AC-ID | 테스트 유형 | 우선순위 | 시나리오 | 기대 결과 | 상태 | 증빙 |
|--------|--------|-------|-----------|---------|---------|----------|------|------|
| TST-012-01 | REQ-012-02 | AC-012-02 | Integration | Critical | **Given** 백엔드 서버가 실행 중이고 상세요구사항 `REQ-001-02`가 서버 state에 존재한다. **When** `DELETE /api/v1/detail/REQ-001-02` 요청을 전송한다. **Then** HTTP 200 응답과 함께 `{ "deleted_id": "REQ-001-02" }` JSON이 반환되고, 이후 `GET /api/v1/details`에서 해당 항목이 조회되지 않는다. | HTTP 200, 응답 `deleted_id == "REQ-001-02"`, 이후 state에서 해당 항목 미조회 | 미실행 | |
| TST-012-02 | REQ-012-02 | AC-012-02 | Integration | Critical | **Given** 백엔드 서버가 실행 중이다. **When** 존재하지 않는 id(`REQ-999-99`)로 `DELETE /api/v1/detail/REQ-999-99` 요청을 전송한다. **Then** HTTP 404 응답과 `{ "code": "NOT_FOUND", "message": "..." }` 형식의 에러 JSON이 반환된다. | HTTP 404, 응답 바디에 `code: "NOT_FOUND"` 포함 | 미실행 | |
| TST-012-03 | REQ-012-01, REQ-012-03 | AC-012-01, AC-012-03 | E2E | Critical | **Given** 상세요구사항 테이블이 화면에 표시되어 있고 행이 3건 이상 있다. **When** 사용자가 특정 행의 삭제 버튼을 클릭한다. **Then** "이 항목을 삭제하시겠습니까?" 확인 다이얼로그가 표시된다. 사용자가 "확인"을 선택하면 해당 행이 테이블에서 즉시 제거되고 `DELETE /api/v1/detail/{id}` API가 호출된다. | 삭제 버튼 클릭 시 confirm 다이얼로그 표시. 확인 선택 시 API 호출 및 행 제거 | 미실행 | |
| TST-012-04 | REQ-012-03 | AC-012-03 | E2E | High | **Given** 상세요구사항 테이블이 화면에 표시되어 있다. **When** 사용자가 특정 행의 삭제 버튼을 클릭하고 확인 다이얼로그에서 "취소"를 선택한다. **Then** `DELETE /api/v1/detail/{id}` API 호출이 발생하지 않고 해당 행이 테이블에 그대로 유지된다. | 취소 선택 시 API 미호출, 행 유지 | 미실행 | |
| TST-SEC-20 | SEC-012-01 | REQ-012-02 | Security | High | **Given** 백엔드 서버가 실행 중이고 `REQ-001-02`와 `REQ-002-01` 두 항목이 서버 state에 존재한다. **When** `DELETE /api/v1/detail/REQ-001-02` 요청을 전송한다. **Then** `REQ-001-02`만 삭제되고, `REQ-002-01`은 state에 그대로 존재한다. 특정 id DELETE가 다른 항목에 영향을 주지 않음을 확인한다. | 요청한 `id`의 항목만 삭제. 나머지 항목 state 불변 | 미실행 | |
| TST-SEC-21 | SEC-012-02 | REQ-012-02 | Security | High | **Given** 백엔드 서버가 실행 중이다. **When** `DELETE /api/v1/detail/../../../etc/passwd` 형식의 경로 순회 문자를 포함한 id로 요청을 전송한다. **Then** FastAPI 경로 파라미터 처리에 의해 요청이 정상 처리되거나 404를 반환한다. `state.delete_detail()` 내부에서 id를 문자열 동등 비교만 사용하므로 경로 순회가 발생하지 않는다. | 경로 순회 문자 포함 id로 요청 시 404 반환 또는 정상 거부. 파일 시스템 접근 없음 | 미실행 | |
| TST-SEC-22 | SEC-012-03 | REQ-012-02 | Security | Medium | **Given** 백엔드 서버가 실행 중이고 상세요구사항이 10건 로드되어 있다. **When** 자동화 스크립트로 `DELETE /api/v1/detail/{id}` 요청을 1초 내 50회 이상 연속 전송한다. **Then** 서버가 크래시 없이 요청을 처리하고, 단일 사용자 로컬 도구이므로 속도 제한 미적용이 현재 설계에서 허용됨을 설계 문서(`SEC-012-03`)로 확인한다. | 50회 연속 요청 처리 시 서버 안정 유지. SEC-012-03 설계 결정 문서 확인 | 미실행 | |

---

### REQ-013: 파일 스냅샷 저장 및 복원

| TST-ID | REQ-ID | AC-ID | 테스트 유형 | 우선순위 | 시나리오 | 기대 결과 | 상태 | 증빙 |
|--------|--------|-------|-----------|---------|---------|----------|------|------|
| TST-013-01 | REQ-013-01 | AC-013-01 | Integration | Critical | **Given** 서버가 실행 중이고 원본 요구사항이 파싱되어 있으며, `session_snapshot.json`이 존재하지 않는다. **When** `POST /api/v1/generate`를 호출하여 상세요구사항 생성이 완료된다. **Then** `backend/data/session_snapshot.json` 파일이 생성되고, 파일 내에 `original_requirements`와 `detail_requirements` 두 키가 존재하며 생성된 항목이 포함된다. | `session_snapshot.json` 생성. 파일에 `original_requirements`, `detail_requirements` 키 존재. `detail_requirements` 항목 수가 생성된 수와 일치 | 미실행 | |
| TST-013-02 | REQ-013-01 | AC-013-01 | Integration | Critical | **Given** `session_snapshot.json`이 이미 존재하는 상태에서 서버가 실행 중이다. **When** `PATCH /api/v1/detail/{id}`로 특정 항목의 `content`를 수정한다. **Then** `session_snapshot.json`이 갱신되고, 파일 내 해당 항목의 `content`가 수정된 값으로 저장되어 있다. 이전 데이터가 잔존하지 않는다. | 수정 후 `session_snapshot.json`의 해당 항목 `content`가 수정값으로 덮어쓰임. 파일 내 이전 값 미존재 | 미실행 | |
| TST-013-03 | REQ-013-01 | AC-013-01 | Integration | Critical | **Given** 상세요구사항 `REQ-001-02`가 서버 state에 존재하고 `session_snapshot.json`이 해당 항목을 포함한다. **When** `DELETE /api/v1/detail/REQ-001-02`를 호출한다. **Then** 삭제 성공(200) 후 `session_snapshot.json`을 읽으면 `REQ-001-02` 항목이 `detail_requirements` 배열에서 제거되어 있다. | DELETE 성공 후 `session_snapshot.json`에 `REQ-001-02` 미포함 | 미실행 | |
| TST-013-04 | REQ-013-02 | AC-013-01 | Integration | High | **Given** `session_snapshot.json`이 특정 내용으로 저장되어 있다. **When** `PATCH /api/v1/detail/{id}`를 두 번 연속 호출한다. **Then** `session_snapshot.json`이 두 번째 수정 내용을 반영한 최신 상태로 단 1개만 존재하고, 첫 번째 수정 이전 데이터가 파일에 잔존하지 않는다. | 2회 수정 후 파일이 최신 상태 1개로 유지됨. 파일 개수 1개 | 미실행 | |
| TST-013-05 | REQ-013-03 | AC-013-02 | Integration | Critical | **Given** 상세요구사항 3건이 포함된 유효한 `session_snapshot.json`이 `backend/data/` 경로에 존재한다. **When** 서버 프로세스를 재시작한다 (또는 `snapshot.load_snapshot()` 함수를 직접 호출한다). **Then** `load_snapshot()`이 `True`를 반환하고, `GET /api/v1/details`로 조회 시 스냅샷에 있던 3건이 반환된다. | `load_snapshot()` 반환값 `True`. 복원 후 `GET /api/v1/details` 응답에 스냅샷 항목 3건 포함 | 미실행 | |
| TST-013-06 | REQ-013-03 | AC-013-02 | Integration | High | **Given** `session_snapshot.json`이 `backend/data/` 경로에 존재하지 않는다. **When** 서버 프로세스가 시작된다 (또는 `snapshot.load_snapshot()`을 직접 호출한다). **Then** `load_snapshot()`이 `False`를 반환하고 서버가 정상 기동되며, `GET /api/v1/details`가 빈 배열을 반환한다. | `load_snapshot()` 반환값 `False`. 서버 기동 중단 없음. 빈 state로 기동 | 미실행 | |
| TST-013-07 | REQ-013-03 | AC-013-02 | Integration | High | **Given** `session_snapshot.json`이 존재하지만 JSON 파싱이 불가능한 손상된 내용을 담고 있다 (예: `{ "broken": [}`). **When** 서버 프로세스가 시작된다 (또는 `snapshot.load_snapshot()`을 직접 호출한다). **Then** `load_snapshot()`이 `False`를 반환하고 예외가 서버 기동을 중단하지 않는다. 서버가 빈 state로 정상 기동된다. | `load_snapshot()` 반환값 `False`. 서버 기동 중단 없음. 빈 state로 기동 | 미실행 | |
| TST-013-08 | REQ-013-04 | AC-013-04 | Integration | High | **Given** 서버가 실행 중이다. **When** 상세요구사항 추가(`/api/generate` 완료), 수정(`PATCH`), 삭제(`DELETE`) 각 API를 호출한다. **Then** 각 호출이 인메모리 `state.py`를 직접 대상으로 처리되며, SQLite, PostgreSQL 등 외부 DB 연결 시도가 발생하지 않는다. 소스 코드에서 DB 임포트(`sqlite3`, `sqlalchemy`, `psycopg2` 등)가 없음을 확인한다. | DB 연결 없음. 모든 I/O는 인메모리 state + `session_snapshot.json` 파일만 사용 | 미실행 | |
| TST-SEC-23 | SEC-013-01 | REQ-013-01 | Security | High | **Given** 서버가 실행 중이다. **When** `snapshot.py`의 `SNAPSHOT_PATH` 상수를 확인하고, 외부 입력(환경변수, API 파라미터)으로 저장 경로를 변경할 수 있는 인터페이스가 존재하는지 소스 코드를 검사한다. **Then** `SNAPSHOT_PATH`가 `snapshot.py` 내부 상수로 고정되어 있고, 경로를 외부에서 주입하는 인터페이스가 없음을 확인한다. | `SNAPSHOT_PATH` 소스 내 상수 고정 확인. 외부 경로 주입 인터페이스 없음 | 미실행 | |
| TST-SEC-24 | SEC-013-02 | REQ-013-03 | Security | High | **Given** 공격자가 `session_snapshot.json` 파일에 Pydantic 스키마를 위반하는 데이터(예: `detail_requirements` 항목에 `malicious_field: "injected"` 추가)를 삽입했다. **When** 서버가 기동되어 `load_snapshot()`이 호출된다. **Then** Pydantic 모델(`OriginalRequirement`, `DetailRequirement`)로 역직렬화 시 스키마 검증을 통과한 필드만 state에 적재되고, 추가 필드(`malicious_field`)가 무시된다. 파싱 실패 시 전체 복원을 취소하고 빈 state로 기동한다. | Pydantic 검증 통과 데이터만 state 적재. 불명확 필드 무시 또는 파싱 실패 시 빈 state 기동 | 미실행 | |
| TST-SEC-25 | SEC-013-03 | REQ-013-01 | Security | Medium | **Given** `session_snapshot.json`이 존재한다. **When** 소스 코드에서 `snapshot.py`가 최신 1개의 파일만 덮어쓰는지 확인한다. **Then** `save_snapshot()`이 동일 경로에 덮어쓰기(원자적 rename 방식)를 사용하므로, 파일이 누적되지 않고 단 1개만 유지됨을 코드에서 확인한다. | 소스 코드에서 원자적 덮어쓰기 패턴 확인. 파일 누적 없음 | 미실행 | |
| TST-SEC-26 | SEC-013-04 | REQ-013-01 | Security | Medium | **Given** 서버가 실행 중이고 이전 비정상 종료로 `session_snapshot.tmp` 임시 파일이 잔존한다. **When** `save_snapshot()`이 호출된다. **Then** `session_snapshot.tmp` 임시 파일이 덮어쓰여 새 내용으로 갱신된 후 `session_snapshot.json`으로 rename된다. 잔존 임시 파일이 `load_snapshot()` 호출 시 참조되지 않는다. `load_snapshot()`은 `.json` 파일만 읽음을 소스 코드에서 확인한다. | `load_snapshot()`이 `.tmp` 파일을 무시함을 소스 코드 확인. 잔존 `.tmp` 파일이 다음 저장 시 자동 정리됨 | 미실행 | |
